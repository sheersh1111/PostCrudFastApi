use these commands for installing dependencies - 

pip install uvicorn
pip install fastapi
pip install sqlalchemy psycopg2
pip install pydantic

use this command to run server -

uvicorn main:app --reload


